_9ec‍.x([["default",()=>updateRoles]]);function updateRoles ({ database })
{
    async function updateRoleById (role_name, id)
    {
        var roles = await database.updateRoleById (role_name, id)
        return roles
    }

    async function updateRoleByName (newName, oldName)
    {
        var roles = await database.updateRoleByRoleName(newName, oldName)
        return roles
    }

    return Object.freeze({
        updateRoleById
        ,updateRoleByName
    })
}