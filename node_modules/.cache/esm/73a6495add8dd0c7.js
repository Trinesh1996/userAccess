_dd8‍.x([["default",()=>makeGetAllUsers]]);function makeGetAllUsers ( { database } )
{
    async function getAllUsersSortedAsc ()
    {
        const users = await database.getAllUsersSortedAsc()
        return users
    }
    async function getAllUsersSortedDesc ()
    {
        const users = await database.getAllUsersSortedDesc()
        return users
    }

    async function getAllUsersSortedByCreatedRecently ()
    {
        const users = await database.getAllUsersSortedByCreatedRecently ()
        return users
    }
    async function getAllUsersSortedByCreatedLast ()
    {
        const users = await database.getAllUsersSortedByCreatedLast ()
        return users
    }

    async function getUserById (id)
    {
        const users = await database.getUserById(id)
        return users
    }

    async function getUserByName (name)
    {
        const users = await database.getUserByName(name)
        return users
    }

    async function getUserBySurname (surname)
    {
        const users = await database.getUserBySurname(surname)
        return users
    }

    async function getUserByNameAndSurname (name, surname)
    {
        const users = await database.getUserByNameAndSurname(name, surname)
        return users
    }

    async function getUserByEmailAddress (email)
    {
        const users = await database.getUsersByEmail(email)
        return users
    }

    async function getUserByNameSurnameAndEmailAddress (name, surname, email)
    {
        const users = await database.getUsersByNameEmailAndSurname(name, surname, email)
        return users
    }

    async function getUserByStreetAddress (street)
    {
        const users = await database.getUsersByStreetAddress(street)
        return users
    }

    async function getUserByCity (city)
    {
        const users = await database.getUsersByCity(city)
        return users
    }

    async function getUserByCountry (country)
    {
        const users = await database.getUsersByCountry(country)
        return users
    }

    async function getUserByCityAndCountry (city, country)
    {
        const users = await database.getUsersByCityAndCountry(city, country)
        return users
    }

    async function getUserCreatedBy (createdBy)
    {
        const users = await database.getUsersCreatedBy(createdBy)
        return users
    }
    return Object.freeze({
        getAllUsersSortedAsc
        ,getAllUsersSortedDesc
        ,getAllUsersSortedByCreatedRecently
        ,getAllUsersSortedByCreatedLast 
        ,getUserById
        ,getUserByName
        ,getUserBySurname
        ,getUserByNameAndSurname
        ,getUserByEmailAddress
        ,getUserByNameSurnameAndEmailAddress
        ,getUserByStreetAddress
        ,getUserByCity
        ,getUserByCountry 
        ,getUserByCityAndCountry
        ,getUserCreatedBy
    })
}