_54e‍.x([["default",()=>makeAddRole]]);function makeAddRole ({ database })
{
    async function getAllRoles ()
    {
        const roles = await database.getAllRoles()
        return roles
    }

    async function getAllRolesByRoleId (id)
    {
        const roles = await database.getRolesById(id)
        return roles
    }

    async function getAllRolesByRoleName (name)
    {
        const roles = await database.getRolesByName(name)
        return roles
    }

    return Object.freeze({
        getAllRoles
        ,getAllRolesByRoleId
        ,getAllRolesByRoleName
    })
     
}