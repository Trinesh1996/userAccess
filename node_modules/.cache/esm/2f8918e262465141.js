_624‍.x([["default",()=>makeDeleteUsers]]);function makeDeleteUsers ( {database} )
{
    async function deleteAllUsers ()
    {
        const users = await database.deleteAllUsers()
        return users
    }

    async function deleteUserById (id)
    {
        const users = await database.deleteUserById(id)
        return users
    }

    return Object.freeze({
        deleteAllUsers,
        deleteUserById
    })
}